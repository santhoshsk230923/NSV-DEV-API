version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  buyer-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MODULE: buyer-service
    environment:
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_JWK_URI: ${JWT_JWK_URI}
    depends_on:
      - mysql
    ports:
      - "8084:8088"

  supplier-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MODULE: supplier-service
    environment:
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_JWK_URI: ${JWT_JWK_URI}
    depends_on:
      - mysql
    ports:
      - "8083:8088"

  supply-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MODULE: supply-service
    environment:
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_JWK_URI: ${JWT_JWK_URI}
    depends_on:
      - mysql
    ports:
      - "8082:8088"

  dispatch-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MODULE: dispatch-service
    environment:
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_JWK_URI: ${JWT_JWK_URI}
    depends_on:
      - mysql
    ports:
      - "8087:8088"

  factory-processing-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MODULE: factory-processing-service
    environment:
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_JWK_URI: ${JWT_JWK_URI}
    depends_on:
      - mysql
    ports:
      - "8086:8088"

  transaction-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MODULE: transaction-service
    environment:
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_JWK_URI: ${JWT_JWK_URI}
    depends_on:
      - mysql
    ports:
      - "8081:8088"

  invoice-with-gst-services:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MODULE: invoice-with-gst-services
    environment:
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_JWK_URI: ${JWT_JWK_URI}
    depends_on:
      - mysql
    ports:
      - "8085:8088"

  invoice-without-gst-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MODULE: invoice-without-gst-service
    environment:
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_JWK_URI: ${JWT_JWK_URI}
    depends_on:
      - mysql
    ports:
      - "8084:8088"

volumes:
  mysql_data:
